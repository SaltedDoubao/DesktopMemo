# 隐私与安全

## 数据存储

DesktopMemo 默认将数据存储在本地 `.memodata` 目录，不会主动上传到网络。

## 日志

应用会在 `.memodata/.logs/` 记录运行日志，用于排查问题。提交 Issue 时如需附带日志，请先检查是否包含个人信息并进行脱敏。

## 云同步（规划中）

若未来启用云同步（例如 MySQL 云存储），会遵循“本地优先、可选同步、明确冲突解决、隐私保护”的原则，详见 [MySQL 集成规范](../MySQL-集成规范.md)。

---

**最后更新**：2025-12-26

